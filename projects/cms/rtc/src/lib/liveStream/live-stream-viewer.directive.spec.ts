import { LiveStreamViewerDirective } from './live-stream-viewer.directive';

describe('LiveStreamViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveStreamViewerDirective();
    expect(directive).toBeTruthy();
  });
});
